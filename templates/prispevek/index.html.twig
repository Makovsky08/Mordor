{% extends 'base.html.twig' %}

{% block title %}Prispevek index{% endblock %}

{% block content %}
    <h1>Prispevek index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datum_vytvoreni</th>
                <th>Tematika</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for prispevek in prispeveks %}
            <tr>
                <td>{{ prispevek.id }}</td>
                <td>{{ prispevek.DatumVytvoreni ? prispevek.DatumVytvoreni|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ prispevek.Tematika }}</td>
                <td>
                    <a href="{{ path('app_prispevek_show', {'id': prispevek.id}) }}">show</a>
                    <a href="{{ path('app_prispevek_edit', {'id': prispevek.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_prispevek_new') }}">Create new</a>
{% endblock %}
