<form method="post" action="{{ path('app_prispevek_delete', {'id': prispevek.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prispevek.id) }}">
    <button class="btn">Delete</button>
</form>
