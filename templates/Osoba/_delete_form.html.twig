<form method="post" action="{{ path('app_osoba_delete', {'id': osoba.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="delete-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ osoba.id) }}">
    <button class="btn btn-danger mt-2">Delete</button>
</form>
